apiVersion: v1
kind: Service
metadata:
  name: {{ include "yandex-messenger-bridge.fullname" . }}
  labels:
    {{- include "yandex-messenger-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yandex-messenger-bridge.selectorLabels" . | nindent 4 }}
