package components

templ IntegrationForm(integration *domain.Integration) {
    <div>
        <h3>
            { if integration != nil { "Редактирование" } else { "Создание" } } интеграции
        </h3>
        <p>Тестовый шаблон</p>
    </div>
}