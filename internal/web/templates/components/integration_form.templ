package components

import (
    "yandex-messenger-bridge/internal/domain"
)

templ IntegrationForm(integration *domain.Integration) {
    <div>
        if integration != nil {
            <h3>Редактирование интеграции</h3>
        } else {
            <h3>Создание интеграции</h3>
        }
        <p>Тестовый шаблон</p>
    </div>
}